import { PagePlaceholder } from "../../components/ui/PagePlaceholder";

export function AlertSettingsPage() {
  return (
    <PagePlaceholder
      title="Alert Settings"
      description="Email alert subscription settings coming in a later milestone."
    />
  );
}
