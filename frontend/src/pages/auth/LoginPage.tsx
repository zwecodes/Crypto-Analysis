import { PagePlaceholder } from "../../components/ui/PagePlaceholder";

export function LoginPage() {
  return (
    <PagePlaceholder
      title="Login"
      description="Supabase Auth login form coming in the next milestone."
    />
  );
}
