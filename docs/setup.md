# Local Setup

1. Clone the repo
2. Copy `.env.example` to `.env`
3. Ask [your name] on Discord (DM) for the real credential values
4. Fill in your `.env` file
5. Never commit `.env` — it's gitignored, keep it that way

## Backend setup
(instructions once backend scaffold exists)

## Frontend setup
(instructions once frontend scaffold exists)